{
  "private": true,
  "main": "gulpfile.js",
  "type": "module",
  "scripts": {
    "test": "xo",
    "build": "gulp dist",
    "release:cws": "cd build && chrome-webstore-upload upload --auto-publish",
    "release": "npm run build && npm run release:cws"
  },
  "devDependencies": {
    "chrome-webstore-upload-cli": "^3.5",
    "del": "^8.0",
    "globals": "^17.6.0",
    "gulp": "^5.0",
    "gulp-zip": "^6.1",
    "xo": "^2.0"
  }
}
